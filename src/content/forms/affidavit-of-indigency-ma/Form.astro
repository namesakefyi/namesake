---
import { getEntry, render } from "astro:content";
import { FormContainer } from "../../../components/forms/FormContainer/FormContainer";
import { getFormPdfMetadata } from "../../../forms/getFormPdfMetadata";

const formEntry = await getEntry("forms", "affidavit-of-indigency-ma");
if (!formEntry) return;
const { Content } = await render(formEntry);
const pdfs = await getFormPdfMetadata("affidavit-of-indigency-ma");
---

<FormContainer
  client:load
  slug="affidavit-of-indigency-ma"
  title={formEntry.data.title}
  description={formEntry.data.description}
  costs={formEntry.data.costs}
  pdfs={pdfs}
  inline
>
  <Content />
</FormContainer>
